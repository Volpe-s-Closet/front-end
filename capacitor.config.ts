import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'volpescloset.missvolpe.com',
  appName: "Volpe's Closet",
  webDir: '.output/public'
};

export default config;
