[variables]
NODE_VERSION = "22.20.0"

[phases.setup]
nixPkgs = ["nodejs_22", "npm"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run generate"]

[start]
cmd = "npx serve .output/public"